const volunteer = "Shadi";
const volunteer = "Abdi";
// Volunteer has already been declared in the first line, therefore it cannot be declared twice.
const volunteer = "Shadi";
volunteer = "Hinde";
// Volunteer is declared as a const so it cannot be changed.
console.log(Math.round(10.3));
// No close bracket at the end of the console.log